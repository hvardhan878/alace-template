export interface DataPoint {
  month: string;
  sales: number;
  revenue: number;
}

export interface Item {
  id: number;
  title: string;
  description: string;
}

export interface ItemInput {
  title: string;
  description: string;
}